[project]
name = "divdag-domain-tiny"
version = "0.1.0"
description = "Tiny demo domain for DivDag: local file batch summarization"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["divdag_kernel"]

[project.entry-points."divdag.domains"]
tiny = "tiny.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tiny"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "README.md"]
