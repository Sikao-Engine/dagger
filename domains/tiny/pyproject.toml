[project]
name = "loom-domain-tiny"
version = "0.1.0"
description = "Tiny demo domain for Loom: local file batch summarization"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["loom_kernel"]

[project.entry-points."loom.domains"]
tiny = "tiny.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tiny"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "README.md"]
