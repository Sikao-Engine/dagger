[project]
name = "noveltool"
version = "0.1.0"
description = "Deterministic CLI for the Dagger novel_digest domain (status/next/fill/check)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["dagger-domain-novel-digest"]

[project.scripts]
noveltool = "novel_digest.noveltool.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Pure packaging shim: no importable code of its own. The console script is
# provided by the `dagger-domain-novel-digest` dependency below.
[tool.hatch.build.targets.wheel]
bypass-selection = true

# Resolve the domain package (and its dagger_* deps) from this repo checkout so
# `uv tool install -e .` works standalone, outside the uv workspace.
[tool.uv.sources]
dagger-domain-novel-digest = { path = "../", editable = true }
dagger_kernel = { path = "../../../packages/dagger_kernel" }
dagger_cli = { path = "../../../packages/dagger_cli" }
