[project]
name = "divdag-domain-novel-digest"
version = "0.1.0"
description = "Novel digest domain for DivDag: chapter summary + timeline + consistency"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["divdag_kernel", "divdag_cli", "jinja2>=3.1"]

[project.entry-points."divdag.domains"]
novel_digest = "novel_digest.plugin:plugin"

[project.scripts]
noveltool = "novel_digest.noveltool.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/novel_digest"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "README.md"]
