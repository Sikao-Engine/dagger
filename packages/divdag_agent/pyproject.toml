[project]
name = "divdag_agent"
version = "0.1.0"
description = "Agent protocol layer for DivDag (backend-agnostic)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "divdag_kernel",
    "httpx>=0.27",
    "anyio>=4.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/divdag_agent"]

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "README.md"]
